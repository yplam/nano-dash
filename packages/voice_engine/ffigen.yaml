# Run with `dart run ffigen --config ffigen.yaml`.
name: VoiceEngineBindings
description: |
  Bindings for `src/voice_engine.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/src/voice_engine_bindings_generated.dart'
headers:
  entry-points:
    - 'src/voice_engine.h'
  include-directives:
    - 'src/voice_engine.h'
ffi-native:
comments:
  style: any
  length: full
